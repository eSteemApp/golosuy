'use strict';

describe('golosuy.new module', function() {

  beforeEach(module('golosuys.new'));

  describe('new controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var newCtrl = $controller('NewCtrl');
      expect(newCtrl).toBeDefined();
    }));

  });
});
