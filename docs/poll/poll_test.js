'use strict';

describe('golosuy.poll module', function() {

  beforeEach(module('golosuy.poll'));

  describe('poll controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pollCtrl = $controller('PollCtrl');
      expect(pollCtrl).toBeDefined();
    }));

  });
});
